STEP-1 

CREATE SCHEMA test


STEP-2

CREATE TABLE `test`.`emp A` (
  `EMPID` VARCHAR(50) NOT NULL UNIQUE,
  `EMPNAME` VARCHAR(60) NULL,
  `MGRID` VARCHAR(50) NULL,
  `DEPTID` VARCHAR(50) NULL,
  `SALARY` FLOAT NULL,
  `DOB` DATETIME NULL,
  PRIMARY KEY (`EMPID`)
  );
 
 
STEP-3

CREATE TABLE `test`.`dept B` (
  `DEPTID` VARCHAR(50) NOT NULL UNIQUE,
  `DEPTNAME` VARCHAR(50) NULL,
  PRIMARY KEY (`DEPTID`)
  );